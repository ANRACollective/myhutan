import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MYHUTAN — Malaysian Jungle Tracker",
  description: "Track the status of Malaysia's forests, companies linked to deforestation, and the ecological cost. Educational, open, and sourced.",
  openGraph: {
    title: "MYHUTAN — Malaysian Jungle Tracker",
    description: "Live deforestation events, company accountability, and forest education for Malaysia.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
